import React, {Component} from 'react';

class Logindialog extends Component {
    render() {
        return (
            <div>
                Login to PARTBOX
            </div>
        );
    }
}

export default Logindialog;